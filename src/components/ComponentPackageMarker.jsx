import React from 'react';

/**
 * Components Package Marker
 * Contains reusable UI components (Buttons, Cards, Modals, Loaders, ErrorAlerts).
 */
export const ComponentPackageMarker = () => null;
export default ComponentPackageMarker;
